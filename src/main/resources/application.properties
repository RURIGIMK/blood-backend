# --- Datasource / JPA ---
spring.datasource.url=jdbc:mysql://localhost:3306/blooddb_new?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=20Aug@usT05

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# --- JWT Secret ---
jwt.secret=MySuperSecretKeyForJwtMustBeLongEnough1234567890

# --- Swagger/OpenAPI ---
springdoc.api-docs.path=/v3/api-docs
springdoc.swagger-ui.path=/swagger-ui.html

# --- Logging ---
logging.level.org.springframework.security=DEBUG
logging.level.ke.blood.blood_backend=DEBUG

# --- Mail (example using Gmail SMTP; replace with real credentials) ---
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=your_email@gmail.com
spring.mail.password=your_email_password_or_app_specific_password
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# --- CORS origins for React frontend ---
frontend.origin=http://localhost:3000

# --- Matching defaults ---
matching.max-distance-km=100.0
